import React from 'react';
import StateDisplay from './StateDisplay';

const QueryDisplay = ({ title, data }) => (
  // This is just a placeholder until the query display is customized further
  <StateDisplay title={title} data={data} />
);

export default QueryDisplay;
